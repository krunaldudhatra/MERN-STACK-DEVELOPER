function discountCalculator(totalAmount) {
    let answer,discount;
    if(totalAmount>=100)
        discount=10
    else if(totalAmount>=0 && totalAmount<=99)
        discount=5
        
    //Write your code below
return answer=totalAmount*(100-discount)/100
}
