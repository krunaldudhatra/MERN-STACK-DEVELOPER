    //you have been provided with two  numbers add them aand store their result in answer
    //hint: use parseFloat for adding
function add(num1, num2) {
    let answer=parseFloat(num1)+parseFloat(num2);
    //Write your code below
    return answer;
  }
  
//You are given two numbers subtract them and store the result in answer;
 function subtract(num1, num2) {
    let answer=parseFloat(num1)-parseFloat(num2);
    //Write your code below
    return answer;
  }
  //  You are given two numbers multiply them and store their result in answer;
  function multiply(num1, num2) {
    let answer=parseFloat(num1)*parseFloat(num2);
    //Write your code below
    return answer;
  }
  //You are given two numbers divide them and store their result in answer
    function divide(num1, num2) {
    let answer=parseFloat(num1)/parseFloat(num2);
    //Write your code below
    return answer;
  }
  //parseFloat() is a JavaScript function used to convert (or "parse") a string into a floating-point number. It reads the string from left to right, stopping at the first character that isn't part of a valid number. If the function cannot find a number at the beginning of the string, it returns NaN (Not-a-Number).
 /* let num1 = parseFloat("3.14"); // 3.14
let num2 = parseFloat("42px"); // 42
let num3 = parseFloat("abc");  // NaN  */
