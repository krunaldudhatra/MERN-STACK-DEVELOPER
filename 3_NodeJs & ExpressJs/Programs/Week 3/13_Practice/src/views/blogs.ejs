<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Blogs List</title>
</head>
<body>
  <h1>All Blogs</h1>
  <ul>
    <% blogs.forEach(blog => { %>
      <li>
        <h2><%= blog.title %></h2>
        <p><%= blog.description %></p>
        <img src="<%= blog.img %>" alt="<%= blog.title %>" style="max-width: 300px;">
        <hr>
      </li>
    <% }); %>
  </ul>
</body>
</html>
